{
  "name": "editorJS",
  "version": "0.1.0",
  "main": "lib/index.js",
  "scripts": {
    "start": "electron .",
    "build:asar": "asar p --unpack build --unpack-dir bin . bin/app.asar"
  },
  "devDependencies": {
    "electron": "21.3.1"
  }
}
